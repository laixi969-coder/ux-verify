---
name: ux-verify
description: 前端改动后浏览器验证。跑 tsc + 测试 + i18n 一致性 + 用户流程走查 + 交互点击 + 文案检查。触发词：验证一下、跑一下 UX、/ux-verify、前端验证、验一下。
---

# 前端验证

前端代码改动完成后，用浏览器实际走一遍完整用户路径，确认交互可用、流程通畅、文案正确。不做代码审查——只验证用户看到和点到的。

## 触发条件

- 用户说"验证一下"、"跑一下 UX"、"前端验证"、"验一下"
- `/ux-verify` 或 `/frontend-verify`
- 任何前端改动完成后

## 工作流

### 0. 前置条件

```bash
# 确认 dev server 在跑，不在就启动
lsof -i :3000 | grep LISTEN || (npm run dev &)
```

如需要登录态且无测试账号，用项目 ORM（Prisma/bcryptjs 等）建一个测试账号，用户名 `qatest` / 密码 `qatest123`。

### 1. 代码质量（自动适配项目）

根据项目实际工具链执行：

```bash
# TypeScript 项目
npx tsc --noEmit --pretty 2>&1 | tail -5

# 运行单元测试（自动检测测试框架）
# vitest
npx vitest run 2>&1 | tail -10
# 或 jest
npx jest --passWithNoTests 2>&1 | tail -10
```

如果项目没有 tsc 或测试框架，跳过对应步骤并在报告中标注 `⚠ 跳过（项目未配置）`。

### 2. i18n 一致性（如果项目有 i18n）

先探测项目 i18n 结构：

```bash
# 查找 i18n 文件位置
find . -path "*/messages/*.json" -o -path "*/locales/*.json" -o -path "*/i18n/*.json" | head -10
```

如果找到 i18n 文件：
- 提取本次改动涉及的所有 i18n key
- 验证每个 key 在所有语言文件中都存在且值有效（不为空、不等于 key 名本身）
- 报告缺失或为空的 key

如果项目没有 i18n，跳过并在报告中标注 `⚠ 跳过（无 i18n）`。

### 3. 浏览器用户流程验证

使用 `$B`（gstack browse 命令）操作浏览器。`$B` 用法参考 gstack skill。

按以下顺序走完完整用户路径，每一步实际点击并检查反馈：

```
登录 → 主页 → 核心页面列表 → 进入详情 → 遍历各子页面/阶段 → 检查导航
```

#### 3.1 登录 + 主页

- 导航到主页，`$B screenshot` 截图存证
- 确认核心导航元素齐全

#### 3.2 核心页面

- 导航到本次改动涉及的页面
- 确认所有导航/标签文案正确
- 确认无已删除功能的残留文案

#### 3.3 遍历子页面/阶段

对每个子页面或阶段检查：

| 检查点 | 方法 | 判定标准 |
|--------|------|----------|
| 页面可访问 | `$B goto URL` 或 `$B click @eN` | 页面切换，URL 正确 |
| 空状态有 CTA | `$B snapshot -i` 提取文案 | 有 actionable 按钮或明确指引 |
| CTA 前置条件检查 | 检查按钮 disabled 状态/渲染条件 | 不满足条件时不展示按钮 |
| CTA 按钮可点击 | `$B click @eN` | 不超时，有状态变化 |
| 点击后有反馈 | 按钮 loading / toast 出现 / 弹窗出现 | 用户能感知操作在进行 |
| 页面可离开 | 点击导航切换到其他子页面 | 能正常切换，不被阻断 |

#### 3.4 交互验证

| 检查项 | 方法 |
|--------|------|
| 按钮实际可点击 | `$B click @eN` 不 timeout |
| 点击后有状态变化 | loading / toast / URL 变化 / 弹窗 |
| 返回/导航可用 | 点击返回，回到正确页面 |
| 无 JS 错误 | `$B console --errors`，过滤已知良性 error |
| 布局不遮挡 | 检查固定定位元素不覆盖内容 |

#### 3.5 文案验证

| 检查项 | 方法 |
|--------|------|
| 导航/标签文案 | `$B js` 提取文字，对比 i18n 文件（如有） |
| CTA 文案 | 与当前页面/阶段匹配，指向正确目标 |
| 无已删除功能残留 | 搜索旧名称/旧术语，确认已清理 |
| 空状态指引 | 根据数据状态展示正确指引 |

### 4. 产品逻辑验证

不只检查按钮能不能点，还要验证逻辑对不对：

| 检查点 | 方法 | 判定标准 |
|--------|------|----------|
| 页面/阶段依赖顺序 | 按导航从左到右遍历 | 前置步骤未完成时，后续指引指向正确前置 |
| 按钮前置条件 | 检查页面 data 状态 | 条件不满足时按钮不显示或 disabled |
| 返回导航正确 | 点击返回/面包屑 | 回到上一级，不是跳到无关页面 |
| 空状态指引准确 | 检查空状态文案 | 指引指向正确的下一步操作 |
| 页面间数据传递 | 完成 A 后进入 B | B 能看到 A 的产出 |

### 5. 报告

```
前端验证报告
═══════════
□ 代码质量检查通过
□ 测试通过（如配置）
□ i18n key 齐全（如配置）
□ 所有页面可访问 & 可离开
□ 空状态有 CTA
□ CTA 有前置条件检查
□ 按钮可点击 + 有反馈
□ 用户流程通顺
□ 布局不重叠/遮挡
□ 文案无过期引用
```

全部 `□` 变 `✅` 才报"完成"。遇到跳过的项标注 `⚠`。

## 边界条件

| 场景 | 处理 |
|------|------|
| 项目无 TypeScript | 跳过 tsc，标注 `⚠` |
| 项目无测试框架 | 跳过测试，标注 `⚠` |
| 项目无 i18n | 跳过 i18n 检查，标注 `⚠` |
| 无法登录（无测试账号） | 提示用户提供账号或创建测试账号 |
| 浏览器操作超时 | 重试 1 次，仍失败则报告具体步骤和错误信息 |
| `$B` 命令不可用 | 提示用户安装 gstack：`cd ~/.claude/skills/gstack && ./setup` |

## 检查点（需用户确认）

- **3.1 之后**：展示主页截图，确认页面正常后继续
- **3.5 之后**：列出发现的文案问题（如有），确认后继续产品逻辑验证
- **5 之前**：展示问题汇总，确认是否需要修复后再验证

## 已知陷阱

**Canvas/transform 层按钮被 DOM 事件拦截**：`pointer-events: none` 或 z-index 问题会导致按钮看起来正常但实际点击穿透到下层元素。tsc 零错误 + 测试全过不代表按钮能点——只有浏览器里 `$B click` 不 timeout 才算数。
